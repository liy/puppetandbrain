import Command from './Command';

export default class RemoveBlock extends Command
{
  constructor(block) {
    super();
    this.block = block;
  }
  
  porcess() {
    
  }

  undo() {

  }

  redo() {

  }
}